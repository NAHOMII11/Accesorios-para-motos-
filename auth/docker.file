# Usar la versión de Node que tienes instalada
FROM node:22-alpine

# Carpeta de la app
WORKDIR /app

# Inicializar y descargar la librería necesaria para Postgres
RUN npm init -y && npm install pg

# Copiar tu script
COPY auth.js .

# Ejecutar el script
CMD ["node", "auth.js"]