server:
  port: 8090

spring:
  application:
    name: catalog-api-gateway
  cloud:
    gateway:
      routes:
        - id: catalog-service-route
          uri: http://localhost:8081
          predicates:
            - Path=/api/catalog/**
          filters:
            - StripPrefix=0